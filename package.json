{
  "name": "blog-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "react-scripts start",
    "build": "CI=false react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^18.3.1",           // Latest stable version
    "react-dom": "^18.3.1",       // Match with react
    "react-router-dom": "^6.23.1", // Latest compatible version
    "axios": "^1.7.2",            // Latest stable version
    "react-scripts": "^5.0.1"     // Keeping 5.0.1 for now, can upgrade to 5.1.0 if needed
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0.0"            // Specify Node.js version for Vercel
  }
}
